name: voiced_point_conversion_for_jp
description: 日本語の清音を濁音、半濁音、小書きに変換するライブラリです.
version: 0.0.2
homepage: "https://github.com/rise-rocktate/voiced_point_conversion_for_jp"

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # For details regarding fonts in packages, see
  # https://flutter.dev/custom-fonts/#from-packages
